# ── Vetology secrets (template) ─────────────────────────────
# Copy this file to `.env` (which is git-ignored) and paste your key.
#
#   cp .env.example .env
#
# Get a PixelLab API key at https://pixellab.ai/account
PIXELLAB_API_KEY=
